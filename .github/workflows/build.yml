name: Build

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      build_matrix: |
        [
          { "board": "prospector", "shield": "totem_dongle" },
          { "board": "seeed_xiao_nrf52840", "shield": "totem_left" },
          { "board": "seeed_xiao_nrf52840", "shield": "totem_right" }
        ]